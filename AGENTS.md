# AGENTS.md

## この文書の役割

- この文書は、エージェントがこのリポジトリで作業する際の追加指示をまとめたものです
- 人間向けの一般ルールや日常運用の正本は `README.md` と `docs/` を参照してください
- 特に開発手順、検証方針、完了条件は `docs/development-guide.md` を正本として扱ってください

## 開発環境

- 本プロジェクトはTypeScriptベースでReactのアプリを作っています
- パッケージ管理は pnpm です。
  - npm script を実行する時は pnpm から実行してください。

## コミニュケーション言語

- 原則として日本語を使います。
  - 過去に英語を優先していた経緯があるので、内部には英語のドキュメントも残っていますが、中期的には日本語化していきます。
  - ただし、コード内の1行程度のコメントに関しては英語で残します。

## コード規約

- JavaScriptではなくTypeScriptを優先的に使用します。
  - ユーザーがJavaScriptの概念を質問するときには「JavaScriptで質問です」と聞くかもしれませんが、回答内容のサンプルはTypeScriptのコードで型をつけた状態で回答してください。
- 関数の定義は function ではなく アロー関数を使ってください。

  ```typescript
  //NG
  function foo() {}
  //OK
  const bar = () => {};
  ```

  - 例外がいくつかあります
    - TanStackRouterのRouteファイルは、`function RouteComponent()` のように function 宣言を使ってください,

- 通常のプロジェクト規約から意図的に外れる実装をする場合は、短いコメントを近くに残してください。
  - 命名、ファイル配置、export 形式、フレームワーク都合の回避策など、理由がコードだけでは伝わりにくいものが対象です
  - コメントには「何をしているか」ではなく、「なぜこの例外が必要か」を書いてください

## テスト・リンター

- Vitest でテストを書いています。
  - テストのファイル名は `fileName.spec.ts(x)` とします。
  - ユニットテストがしやすいように関数を書いてください
  - ReactComponentで使用するロジックはコンポーネントと同じファイル内にできるだけ pure function として作成しつつ `__TEST__ComponentName` というオブジェクトにまとめてエクスポートすることで内部関数の維持とユニットテストを同居させます
- ドキュメントのみの変更では、原則としてコードの `test` / `lint` / `type-check` を必須にしません
- コード変更を含む場合の検証方針は `docs/development-guide.md` を参照してください
- ignore系のコメントコードは後述する例外を除いて禁止します。
  - エラーを消すことが困難な場合はその旨を報告してください

### `eslint-disable-next-line` を許容するパターン

- react-refresh/only-export-components
  - export の対象がテスト用途に限定されている場合
    - 多くのケースで `__TEST__` の接頭辞をつけています

## Skills

- `docs/agents/skills` フォルダ以下に詳細な指示をまとめています
  - 指示のキーワードが含まれているので必ず先に読むようにしてください

## 参照先

- `README.md`: プロジェクトの入口
- `docs/development-guide.md`: 開発手順、検証方針、完了条件
- `docs/naming-convention.md`: 命名規約
- `docs/spec.md`: 技術構成の入口
- `docs/decisions/README.md`: 設計判断ログの運用ルール

## その他

- 変更・作成したファイル全体の出力は不要です。
  - ファイルパスの一覧だけを出力してください。
