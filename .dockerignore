# Ignore everything
**

# Allow only what `pnpm install` + `pnpm build` (tsc -b && vite build) and the runtime stage need
!index.html
!nginx
!package.json
!pnpm-lock.yaml
!public
!search
!src
!tsconfig.app.json
!tsconfig.json
!tsconfig.node.json
!vite.config.ts
