# Refuse packages published within the last 7 days (10080 minutes)
# as a cooldown against short-lived supply-chain attacks on npm.
# Requires pnpm >= 10.16.0 (silently ignored on older versions; default becomes 1440 in 11.0).
minimumReleaseAge=10080
