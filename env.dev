# === Environment ===
DDBJ_SEARCH_ENV=dev

# === Application Settings ===
# ブラウザから ES/API を叩くベース URL
# dev では production を参照 (ReactiveSearch が /search/resources を必要とするため)
# ES 直接アクセスを API 移行後は http://localhost:8000 に変更可能
DDBJ_SEARCH_BASE_URL=https://ddbj.nig.ac.jp

# === Command ===
DDBJ_SEARCH_FRONT_COMMAND=sleep infinity
